on: push

name: Slack Notification

jobs:
  slack-notification:
    runs-on: ubuntu-latest

    env:
      SLACK_CHANNEL: ci-cd
      SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK }}
      SLACK_ICON: https://avatars2.githubusercontent.com/u/44036562?s=400&v=4
      SLACK_USERNAME: CI Bot
      SLACK_MESSAGE_TEMPLATE: <https://github.com/${{github.repository}}/pull/${{github.event.pull_request.number}} | Link to PR ${{github.event.pull_request.number}}>

    steps:
      - uses: actions/checkout@v1

      - uses: rtCamp/action-slack-notify@v2.0.2
        name: Slack Success Notification
        if: success()
        env:
          SLACK_TITLE: Success
          SLACK_COLOR: '#1E824C'
          SLACK_MESSAGE: Success ☀️! `${{ env.SLACK_MESSAGE_TEMPLATE }}`

      - uses: rtCamp/action-slack-notify@v2.0.2
        name: Slack Failure Notification
        if: failure()
        env:
          SLACK_TITLE: Fail
          SLACK_COLOR: '#F83535'
          SLACK_MESSAGE: Build Failed 🌧️ `${{ env.SLACK_MESSAGE_TEMPLATE }}`

      - uses: rtCamp/action-slack-notify@v2.0.2
        name: Slack Stopped Notification
        if: cancelled()
        env:
          SLACK_TITLE: Cancel
          SLACK_COLOR: '#ADADAD'
          SLACK_MESSAGE: Build Stopped ✋🏻 `${{ env.SLACK_MESSAGE_TEMPLATE }}`
