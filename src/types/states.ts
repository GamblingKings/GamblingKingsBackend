/* ----------------------------------------------------------------------------
 * States
 * ------------------------------------------------------------------------- */

export enum UserStates {
  CONNECT = 'CONNECT',
  DISCONNECT = 'DISCONNECT',
}

export enum GameStates {
  CREATED = 'CREATED',
  DELETED = 'DELETED',
}
