import * as LambdaTester from 'lambda-tester';
import { broadcastConnections } from '../../utils/broadcast';

/* ----------------------------------------------------------------------------
 * Test broadcastConnections
 * ------------------------------------------------------------------------- */
describe('test broadcastConnections', () => {
  beforeEach(async () => {
    console.log('test');
  });

  afterEach(() => {
    console.log('test');
  });

  test('test', () => {
    expect(1).toBe(1);
  });
});
