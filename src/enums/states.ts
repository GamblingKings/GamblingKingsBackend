/* ----------------------------------------------------------------------------
 * States
 * ------------------------------------------------------------------------- */

export enum UserStates {
  CONNECTED = 'CONNECTED',
  DISCONNECTED = 'DISCONNECTED',
}

export enum GameStates {
  CREATED = 'CREATED',
  DELETED = 'DELETED',
}
