/* ----------------------------------------------------------------------------
 * States
 * ------------------------------------------------------------------------- */

export enum UserStatesEnum {
  CONNECTED = 'CONNECTED',
  DISCONNECTED = 'DISCONNECTED',
}

export enum GameStatesEnum {
  CREATED = 'CREATED',
  DELETED = 'DELETED',
}
